import type { Enrollment } from "@/types";

/**
 * Production seed contains no sample enrollments — the admin dashboard starts
 * clean and only fills with real training registrations submitted by visitors.
 */
export const enrollments: Enrollment[] = [];
